# Deploy dependency list — platforms like Streamlit Community Cloud read this file.
# Local source of truth is pyproject.toml + uv.lock (uv sync); keep this mirror in step.
streamlit>=1.40
# Search itself uses only the Python standard library (sqlite3 with FTS5) — no extra deps.
# The Topic Cloud tab renders as styled HTML, so it needs no extra deps either.
